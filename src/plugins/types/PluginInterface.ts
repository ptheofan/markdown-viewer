/**
 * Re-export plugin types from shared for use in plugins
 */
export type {
  PluginMetadata,
  PluginOptions,
  MarkdownPlugin,
  PluginManagerConfig,
  PluginLoadResult,
  PluginRegistrationEvent,
} from '@shared/types/plugin';
